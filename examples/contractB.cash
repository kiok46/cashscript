pragma cashscript ^0.10.0;

contract ContractB(bytes20 pkh) {
    function dummyFuncA() {
        int d = 0;
        require(2==2);
    }
    
    function dummyFuncB() {
        require(2==2);
    }

    function trigger(pubkey pk, sig s) {
        int m = 1;
        console.log("ContractB 'trigger' function called.");
        require(hash160(pk) == pkh);
        require(checkSig(s, pk));
    }
}
